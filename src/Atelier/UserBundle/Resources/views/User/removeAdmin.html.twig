{% extends "AtelierUserBundle::layout.html.twig" %}

{% block title %}
  Remove the admin right - {{ parent() }}
{% endblock %}

{% block user_body %}
    <h3>Remove the admin right</h3>
 
  <p>
    Are you sure you want to remove the admin right to the user "{{ user.username }}" ?
  </p>
 
  <form action="{{ path('atelier_user_removeAdmin', {'id': user.id}) }}" method="post">
    <a href="{{ path('atelier_user_show', {'id': user.id}) }}" class="btn">
      <i class="icon-chevron-left"></i>
      Go back to the {{user.getUsername()}} profile
    </a>
    <input type="submit" value="Remove admin right" class="btn btn-danger" />
    {{ form_rest(form) }}
  </form>
{% endblock %}
