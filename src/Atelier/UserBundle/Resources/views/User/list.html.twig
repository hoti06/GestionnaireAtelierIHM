{% extends "AtelierUserBundle::layout.html.twig" %}

{% block user_body %}
     {% for membre in list %}
         {% if (membre.hasRole('ROLE_SUPER_ADMIN') == false) %}
         	<h2>{{ membre.getUsername() }}</h2>
         	<a href="{{ path('atelier_user_show', {'id': membre.getId()}) }}" class="btn"s>show</a>
     	 {% endif %}
     {% endfor %}
     <a href="{{ path('fos_user_security_logout') }}" class="btn">logout</a>
{% endblock %}
